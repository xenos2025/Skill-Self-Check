# Skill 自检报告（普通用户版）

**检查对象：** `<Skill 名称>`  
**检查日期：** `<YYYY-MM-DD>`  

## 一句话结论

# **不是标准 Skill 包 / 需要先修改 / 达到基础使用门槛 / 可以进入受控试用**

`<用一句普通话说明最重要的原因。不要直接写 Critical、JSON、exit code 或 ship floor。>`

## 现在能不能用

| 项目 | 结果 | 普通话说明 |
|------|------|------------|
| Skill 包结构 | 有效 / 无效 | 是否只有一个正确根目录，名称、路径、文件和资源能否整体安装 |
| 基础结构 | 通过 / 未通过 | 名称、说明和主要步骤是否完整 |
| 工作说明 | 清楚 / 有缺口 | 何时使用、何时不用、怎么验收是否说清 |
| 配套材料 | 齐全 / 不齐 / 不需要 | 资料、案例、记录方式和辅助程序是否够用 |
| 本次认证/打分流程 | 已跑 / 未跑 | 是否已跑结构检查、安全预检、成绩单（企业主线） |
| 作者进阶行为证据 | 已附 / 未附（可选） | 高级审计用的行为 JSON；未附不等于企业主线失败 |
| Token 消耗 | 估算 / 已测 / 未评估 | 静态值只估算说明书输入，不代表完整对话成本 |
| 运行时长 | 已测 / 未测 | 未附作者进阶证据时通常写“未测” |
| 循环护栏 | 有护栏 / 有风险 / 不涉及 | 重试类指令有没有“最多几次、超时或转人工”的停止条件 |

如果“Skill 包结构”为无效，后续分数只能解释为局部文件检查，不得给成熟度等级、
能力类型、徽章或“可以交付”的结论。

## 必须先解决的问题

`<没有就写：本次没有发现阻止基础使用的问题。>`

### 1. `<普通话标题>`

- **发生了什么：** …
- **为什么重要：** …
- **建议怎么改：** …

## 建议尽快改进

- `<每条使用“问题 → 影响 → 改法”，不展示内部字段名。>`

## 还需要你确认

| 要确认什么 | 我建议的答案 | 状态 |
|------------|--------------|------|
| … | … | 待回答 / 已回答 |

## 这次改动的效果（只在我替你改过之后填）

改完之后我重新跑了一遍同样的检查，下面是前后对比，不是我的印象：

| 看什么 | 改之前 | 改之后 |
|--------|--------|--------|
| 能不能用（基础门槛） | 未通过 / 通过 | 未通过 / 通过 |
| Skill 包结构 | 无效 / 有效 | 无效 / 有效 |
| 必须先解决的问题 | `<n>` 个 | `<n>` 个 |
| 建议改进的问题 | `<n>` 个 | `<n>` 个 |

- **修好了：** `<用普通话列出解决了哪些问题>`
- **新出现的：** `<这轮改完才发现的问题；说明是这次改坏的，还是补齐结构之后才轮到检查的>`
- **还没解决：** `<剩下的，以及为什么这轮先不动>`

`<某一行的满分变了（例如补上步骤之后才需要配套材料）时，直接说明范围变化，不要说成分数涨了或跌了。>`

## 下一步

`<只给一个最重要的下一步，例如：先补齐验收标准，再重新检查。>`

## 这份检查不能证明什么

- 基础门槛通过，不代表真实运行一定正确或安全。
- 本报告不是特定 AI 平台的兼容性认证。
- 涉及外部发送、业务数据或真实客户时，还需要单独的行为与安全验证。
